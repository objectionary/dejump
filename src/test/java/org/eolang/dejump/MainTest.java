package org.eolang.dejump;

import java.io.File;
import org.hamcrest.MatcherAssert;
import org.hamcrest.Matchers;
import org.junit.jupiter.api.Test;
import picocli.CommandLine;

public final class MainTest {

    //@Test
    /*public void testCLI() {
        final String[] args = {"--format", "eo", "temp.eo"};
        Main main = new Main();
        new CommandLine(main).parseArgs(args);
        MatcherAssert.assertThat(main.format, Matchers.is("eo"));
        MatcherAssert.assertThat(main.file, Matchers.is(new File("temp.eo")));
    }*/
}
